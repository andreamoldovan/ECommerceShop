@page
@model ECommerceShop.Pages.Coffeee.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Coffee</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Coffee.ID" />
            <div class="form-group">
                <label asp-for="Coffee.Name" class="control-label"></label>
                <input asp-for="Coffee.Name" class="form-control" />
                <span asp-validation-for="Coffee.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Coffee.Feature" class="control-label"></label>
                <input asp-for="Coffee.Feature" class="form-control" />
                <span asp-validation-for="Coffee.Feature" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Coffee.Price" class="control-label"></label>
                <input asp-for="Coffee.Price" class="form-control" />
                <span asp-validation-for="Coffee.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Coffee.ArrivingDate" class="control-label"></label>
                <input asp-for="Coffee.ArrivingDate" class="form-control" />
                <span asp-validation-for="Coffee.ArrivingDate" class="textdanger"></span>
            </div>
            <label asp-for="Coffee.CustomerID" class="control-label">Customer Name</label>
            <select asp-for="Coffee.CustomerID" class="form-control" asp-items="ViewBag.CustomerID">
            </select>
            <span asp-validation-for="Coffee.CustomerID" class="text-danger"></span>
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;
                                foreach (var cat in Model.AssignedCategoryDataList)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedCategories"
                                               value="@cat.CategoryID"
                                               @(Html.Raw(cat.Assigned ? "checked=\"checked\"" : "")) />
                                        @cat.CategoryID @: @cat.Name
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>

                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
